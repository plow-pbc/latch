# Pinned to the versions the camoufox-cli reference install was proven on.
# The version coupling is strict (see UPSTREAM.md); bump both together
# with runtime.lock.json or not at all.
#
# scripts/build-browser-runtime.mjs resolves this to a fully hash-pinned
# per-arch wheel set (pip download + verification against runtime.lock.json).
camoufox[geoip]==0.5.4
playwright==1.60.0
