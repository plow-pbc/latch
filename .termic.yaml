version: 1
scripts:
  # worktree-setup.sh with a donor named for it — it never infers one (see its
  # donor block), and a Termic worktree without one starts with no browser and
  # no vault. The hook works out which checkout that is; the reasoning is there.
  setup: scripts/termic-setup.sh
  run: just app
  archive: scripts/worktree-cleanup.sh
  preview_url: ''
  files_to_copy: []
  run_scripts: []
sandbox:
  enabled_by_default: false
  allowed_hosts: []
  allowed_paths: []
exclude: []
